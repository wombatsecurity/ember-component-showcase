{{#if hasBlock}}
  <pre data-language={{language}}>
    <code class={{languageClass}}>
      {{yield}}
    </code>
  </pre>
{{else}}
  <pre data-language={{language}}>
    <code class={{languageClass}}>
      {{src}}
    </code>
  </pre>
{{/if}}

