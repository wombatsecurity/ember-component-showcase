{{#if sourceId}}
	{{#showcase/s-tabs (array (hash title=title language=language icon=icon) ) as |tab| }}
		{{from-elsewhere name=sourceId}}
	{{/showcase/s-tabs}}
{{else}}
	{{showcase/s-tabs (array (hash title=title language=language src=src icon=icon) ) }}
{{/if}}